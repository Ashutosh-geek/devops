- name: Configure system for Docker application
 hosts: localhost
connection: local
become: false
tasks:

- name: Display configuration message

 debug:

 msg: &quot;System configured for Docker application&quot;